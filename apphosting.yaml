backends:
  - id: backoffice-backendvla
    sourceDirectory: backend
    runtime: nodejs20
    scripts:
      buildCommand: npm install
      runCommand: npm start
    env:
      - variable: PORT
        value: "8080"
      - variable: NODE_ENV
        value: "production"
