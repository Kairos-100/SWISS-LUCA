backends:
  - id: backoffice-backendvla
    sourceDirectory: backend
    runtime: nodejs20
    scripts:
      buildCommand: npm ci --production || npm install --production
      runCommand: node server.js
    env:
      - variable: PORT
        value: "8080"
      - variable: NODE_ENV
        value: "production"
